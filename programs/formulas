def show_algebra_formulas():
    print('Algebra Formulas:')
    print("1. Quadratic Formula:\n ax^2 + bx + c = 0")
    print("2. Distance Formula: \nsqrt((x2 - x1)^2 + (y2 - y1)^2)")
    print("3. Slope of a Line:\n m = (y2 - y1) / (x2 - x1)")
    print("4. Point-Slope Form:\n y-y1 = m(x-x1)")
    print("5. Slope-intercept Form:\n y = mx + b")
    # Add more algebra formulas as needed

def show_geometry_formulas():
    print("\nGeometry Formulas:")
    print("1. Area of a Rectangle: A = lw")
    print("2. Area of a Triangle: A = (1/2)bh")
    print("3. Area of a Circle: A = πr^2")
    print("4. Circumference of a Circle: C = 2πr")
    print("5. Distance Formula:\n sqrt((x2 - x1)^2 + (y2 - y1)^2)")
    print("6. Point-Slope Form:\n y-y1 = m(x-x1)")
    print("7. Slope-intercept Form:\n y = mx + b")
    # Add more geometry formulas as needed

def show_chemistry_formulas():
    print("\nChemistry Formulas:")
    print("1. Ideal Gas Law: PV = nRT")
    print("2. Mole Calculation: n = m / M")
    print("3. Dilution Equation: C1V1 = C2V2")
    # Add more chemistry formulas as needed

def show_trig_formulas():
    print("\nTrigonometry Formulas:")
    print("1. Pythagorean Theorem: sqrt(a^2 + b^2)")
    print("2. Sine Formula: \nsin(angle) = opposite/hypotenuse")
    print("3. Cosine Formula: \ncos(angle) = adjacent/hypotenuse")
    print("4. Tangent Formula: \ntan(angle) = opposite/adjacent")
    print("5. Cosecant Formula: \ncsc(angle) = adjacent/hypotenuse")
    print("7. Cotangent Formula: \ncot(angle) = adjacent/hypotenuse")
    # Add more trigonometry formulas as needed

def show_sohcahtoa_formulas():
    print("\nSOH-CAH-TOA Formulas:")
    print("Sine formula: \nsin(angle) = opposite/hypotenuse")
    print("Cosine formula: \ncos(angle) = adjacent/hypotenuse")
    print("Tangent formula: \ntan(angle) = opposite/adjacent")
    # Add more SOH-CAH-TOA formulas as needed

def main():
    print("Select a category:")
    print("1. Algebra")
    print("2. Geometry")
    print("3. Chemistry")
    print("4. Trigonometry")
    print("5. SOH-CAH-TOA")

    category_choice = input("Enter your choice: ")
    if category_choice == '1':
        show_algebra_formulas()
    elif category_choice == '2':
        show_geometry_formulas()
    elif category_choice == '3':
        show_chemistry_formulas()
    elif category_choice == '4':
        show_trig_formulas()
    elif category_choice == '5':
        show_sohcahtoa_formulas()
    else:
        print("Invalid choice. Please enter 1, 2, or 3.")

main()
